# 运行协调层（M2）

M1 仅登记边界，当前不编译运行实现。`RunCoordinator` 独占用户级运行和恢复决策；
`ExecutionSession` 拥有一次有限业务单元的 Maa 对象及业务终态，不复制 Pipeline 节点调度。
Maa 状态与业务终态分别保存。必须正常等待在途动作、停止回调，再释放资源。
这里不得出现 WVD 的技能、地图、宝箱判断，也不得读取 Tk 状态。
