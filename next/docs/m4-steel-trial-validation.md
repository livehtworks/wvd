# M4 钢试炼源码扩展

固定旧源`6585f407`的`steeltrail`分支（4254行起）。这是源码能力，不在基础58项任务
目录中；测试使用独立显式扩展定义，不修改正式任务、旧mod或用户配置。

## 实现边界

- `tasks/steel_trial`负责公会/考试菜单、Steel新帧偏移306/258、四点、返回与住宿子图。
  四点保持131/769、827/447、131/769、719/1080和原地图方向。
- ready/noneed/quit通过冻结的SteelTrial对话策略处理，不污染其它任务默认对话。
- `quests/SteelTrial`只持有阶段、选择意图和已确认周期；输入前记录意图，未知结果或
  停止后不自动再次选择。运行计数与付款分别由既有Run状态和住宿回执持有。
- 保留旧`(counter-1) % (REST_INTERVEL+1)`规则，首轮住宿，不受ACTIVE_REST开关影响。
  负间隔明确拒绝；四点不齐、应住宿但未付款，不允许完成。
- 原后续菜单/住宿助手含盲点1/1；新链只在明确地图时关闭地图，明确对话/战斗时交给
  既有子图。未知返回画面有界退出，不能假定已经回城。这一差异仍需离线及后续实机验证。
- 大小写资源沿用封存别名`Steel.png -> steel.png`，没有复制第二张大小写文件。

## 当前验证

实现已接入独立CLI和真实Maa离线夹具。`695295d`及专用对话同步匹配修正构建通过；
`m4-steel-correction-core-regression.log`中状态36方法、计划9方法全部通过，
含三周期两次住宿、恢复意图与幂等、独立扩展真实manifest闭包及Steel大小写别名。
同命令M2运行组因测试入口导入方式错误未加载；之后按discover入口补跑104方法通过，
日志`m4-steel-correction-m2.log`，不是把加载错误当成通过。
首轮`m4-workflow-7g_0bqno`两方法均在发布前因夹具缺bondmate_close被拒绝，连接/输入均0，
没有执行原生任务。共享专用对话会声明该关闭资源，已给夹具补齐，生产资源清单不变。
发布拒绝的EXE哈希、零连接与零输入另行核对；不能向verify_workflows传这些无Run结果冒充静止证明。
补齐夹具后`m4-workflow-vg19i0vi`：选择拒绝Failed4输入、停止UserStopped4输入通过；
完整路线5输入后在Dungeon_Route0_Search0遇到SCENE_UNCONFIRMED，仍未完成32输入链。
同批五个原生Run已独立核对EXE身份、输入数、无坐标不匹配、进程正常退出与quiescent。
不能以选项关闭成功代替路线通过。
后续仍需未知页、恢复、连续周期和mod矩阵。基础完整任务分母保持0/58。
