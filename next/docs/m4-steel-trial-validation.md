# M4 钢试炼源码扩展

固定旧源`6585f407`的`steeltrail`分支（4254行起）。这是源码能力，不在基础58项任务
目录中；测试使用独立显式扩展定义，不修改正式任务、旧mod或用户配置。

## 实现边界

- `tasks/steel_trial`负责公会/考试菜单、Steel新帧偏移306/258、四点、返回与住宿子图。
  四点保持131/769、827/447、131/769、719/1080和原地图方向。
- ready/noneed/quit通过冻结的SteelTrial对话策略处理，不污染其它任务默认对话。
- `quests/SteelTrial`只持有阶段、选择意图和已确认周期；输入前记录意图，未知结果或
  停止后不自动再次选择。运行计数与付款分别由既有Run状态和住宿回执持有。
- 保留旧`(counter-1) % (REST_INTERVEL+1)`规则，首轮住宿，不受ACTIVE_REST开关影响。
  负间隔明确拒绝；四点不齐、应住宿但未付款，不允许完成。
- 原后续菜单/住宿助手含盲点1/1；新链只在明确地图时关闭地图，明确对话/战斗时交给
  既有子图。未知返回画面有界退出，不能假定已经回城。这一差异仍需离线及后续实机验证。
- 大小写资源沿用封存别名`Steel.png -> steel.png`，没有复制第二张大小写文件。

## 当前验证

实现已接入独立CLI和真实Maa离线夹具，尚未构建/执行，不能登记通过。
待验：完整32输入、选择停止/拒绝、三周期状态与间隔、独立扩展资源闭包；
后续仍需未知页、恢复、连续周期和mod矩阵。基础完整任务分母保持0/58。
