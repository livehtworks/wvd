# 运行链事故整改：本地限定收口

## 结论

本轮按 `WVD_Runtime_Chain_Repair_79870e4_20260922` 工作包完成源码集成、真实 Windows 目标构建和限定实机启动链检查。当前完整任务仍为 **`NOT_DELIVERABLE`**，但既定 MuMu 已真实完成繁中下载弹窗、标题页、王城停点和零输入安全流程；不再沿用“没有运行候选服务”的旧结论。

## 基线与应用

- 本次整改的 Git 基线：`79870e4acfb37865b8169e1e7e844d4ca8f220d9`，分支 `agent/local-stability-notes`。
- 工作包只读候选核对后，通过其事务脚本应用全部 24 个源路径；另在正式 Application 装配测试中补充本轮验收断言。
- 未修改旧 `src/`、`config.json`、`mod`、旧 `dist` 或用户 `.vscode/`；本报告记录提交前的限定验收状态。

## 构建身份

- Windows 目标：`next/build/Release/automationd.exe`。
- SHA256：`422116F743102C4DB794D1850D5FBC7887BFCD24B9E1F0568B6B9FD0B3CE8342`。
- 核心构建 ID：`cbfa676f93590a4fd799f9bb34849c522bc1061d3e09589d0371af78b62eb87e`。
- 工具链：Visual Studio 2022 x64；固定 MaaFramework 5.13.0 SDK 根身份 `179b51b6211fc74317c8dc06f10fae4724aca370df7d1bf694cd00d06ff38efe`；同 SDK 的 OpenCV 4.12 运行库。
- Vue 正式构建通过；未执行打包或安装器。后续限定实机收口启动了候选服务并连接既定设备，范围见本文“2026-09-23 实机收口”。

## R01-R07 结果

- 输入、转场、业务确认：`GuardedAction` 只提交一次输入并生成绑定回执；`AwaitTransition` 独立轮询并消费回执；NoHit 不重放输入，未消费回执不能完成会话。
- 正式发布链：输入图在发布后生成唯一 `__wvd_observe__...` 节点；原延迟只在观察前执行一次；原后继和错误出口由观察节点承接；子流程重置清单包含派生节点；revision 身份取实际拆分图和 `input_contract=2`。
- 受控 4253ms 情形：工作包组件检查证明输入后 4253ms 的有效页面证据仍可确认。该结果使用受控 Observation，不是实机、模板质量或网络验收。
- Boot：当前提示未消失不能算进展；未知/加载画面只回到可取消轮询；Boot 使用一次 120 秒阶段截止，重入不续期；稳定城市仍由原业务图确认。
- 输入前复核：不再第二次截图，只核对连接、前台和 Android 默认显示 viewport；冲突、未知格式、非零原点和方向不确定均拒绝输入。
- 自动连接：正式任务和作者流程开始请求先返回 `preparing`，再由现有设备作业按冻结配置连接选定设备；已连接身份不同会明确拒绝，生命周期段操作前重连当前后端。此顺序除源码与装配核对外，已由既定 MuMu 的王城只读流程实测。
- 蝎女入口：Start/PrepareLeap 接受安全城市或因果轮页；从城市执行旧 `[1,1]` 保底的分支限制为 25 次、每次 1 秒，并排除地图、地下城、战斗、宝箱和阻塞层；进入准备不等于跳跃完成。
- 界面与终态：派生观察节点映射回作者源节点并显示“等待页面结果”；输入事件写 `business_confirmed=false`；取消和清理状态不冒充成功。

## 限定检查

- `automationd` 正式 Windows 目标：构建通过，仅有既存常量条件编译警告。
- `test_t0_startup`：`6294` 条断言、正式发布图 `1516` 个节点，退出码 `0`。覆盖任务/作者流程正式装配、输入/观察闭合、Boot 阶段、城市入口、viewport 解析和包内生命周期计划。
- 工作包组件检查：`19` 项通过；真实设备 `false`、Maa 加载 `false`、产品构建字段 `false`。它只覆盖受控门禁、回执、4253ms、停止、阶段预算、拆图和元数据解析；产品构建由上一条独立完成。
- `git diff --check` 无差异错误。没有运行 196 项矩阵、285 轮资源测试、全任务回归或历史失败重放。

## 2026-09-23 实机收口

- 繁中下载页的 `開始下載` 使用独立位置模板，真实命中并点击后确认弹窗消失；复合语言条件不再充当点击目标。
- 进入繁中王城后，旧英文城市文字模板不能确认停点。具体王城身份改由固定塔楼天际线 ROI 只读确认；各城市共用的旅店、公会、铁匠铺图标不参与具体地点判断。
- 第一轮王城模板已真实 `Hit`，随后因 `/lifecycle_recovery_active` 遗漏业务条件白名单安全失败，且游戏输入为零。该状态本来已由 `WvdRunState` 产生，修复只补齐允许读取的布尔字段。
- 第二轮运行目录 `%LOCALAPPDATA%\WvdNext\runs\16464253-853D-4E92-907F-5165118A326E\1`：状态 `Completed`，王城背景命中，`attempted=0 / accepted=0 / backend_called=0 / rejected=0`，`quiescent=true`。
- 王城地点证据随后统一为 `vision::royal_city()`：世界旅行的内外两层到达条件，以及蝎女、牛洞、分离洞窟、黄金宝箱和 7000G 的王城业务检查点都使用塔楼背景。公共 `Inn/guild` 只保留为城市或菜单操作条件，不能产生“已到王城”事实。
- 验证后服务正常输出 `STOPPED`。没有重启模拟器、进入建筑、执行任务正文或消费游戏资源。

## R08 与未闭合项

- `AdbBackend::wait` 和部分 Maa/Controller 等待或销毁仍可能永久阻塞。本轮没有 detach、强杀、提前释放 SDK 对象或伪报 `quiescent=true`；因此异常停止和长期恢复仍不放行。
- 真实繁中下载、标题页和稳定王城已执行；NEXT、Pause 和任何完整游戏任务仍未由本轮验证。当前 440 张固定资源中的大量英文文字模板尚未具备繁中等价能力，不能从启动页两张新增模板外推全任务可用。
- 历史 `RESOURCE_UNRESOLVED` 保留，本轮没有重复内存长跑。

## 下一步唯一允许的实机验收

启动至稳定王城的限定链已经通过，不再为同一结论重复实机运行。后续仍须按用户明确范围逐项补齐繁中识别资源并验证一个安全任务步骤；在此之前不得把完整任务标记为可用，也不得擅自跳跃、领取、购买、开箱、战斗或运行蝎女整任务。

本节记录该工作包结束时的权限范围。用户后来已明确授权蝎女任务实机执行；后续进展和剩余阻断见仓库 `docs/project-status.md`。
