# M4 状态测试的停止边界

## 已观察事实

`m4-dialogue-payment-state.log`，私有根 `m4-state-tests-mmhhh7di`：20方法6.573秒，
19通过、1失败。住宿待定状态断言通过；原停止测试收到 `Failed / STOP_TIMEOUT`，
不是预期的UserStopped。零底层输入、零完成段，最终真实静止，未启动下一段。

原测试在Run状态成为Running后立即停止，没有证明已经进入协作Wait回调。
本轮事件：61.64ms开始截图，68.66ms请求停止，271.75ms报告超时，1061.91ms原生节点返回，
1068.86ms确认静止。固定200ms停止预算未满足；不宣称任意原生等待可及时取消。

## 定点修正与验收边界

协作停止测试改为等待真实Wait回调发出进入事件后停止，仍要求UserStopped、零下一段。
额外增加受控截图阻塞：停止超时前不释放截图，必须保留quiescent=false和第一代所有权；
测试释放本次离线截图后才可真正静止，终态仍保留Failed/STOP_TIMEOUT，不能改成成功停止。
只调整隔离测试的停止时点与故障注入，不改生产停止预算、终态或SDK等待。

修正一21方法11.467秒，20通过、协作停止1项夹具错误：Context会为业务事件添加
`business.` 前缀，轮询遗漏该前缀导致 `CHECKPOINT_TIMEOUT`，不是游戏输入或停止成功。
私有根 `m4-state-tests-k7tohd39`，日志 `m4-stop-boundary-correction1.log`；新增截图阻塞
所有权测试已通过。修正二仅对齐实际事件名，待构建复验。两轮后不重复运行挑PASS。
M2的102项同产物回归已通过，但不替代上述M4失败记录。
