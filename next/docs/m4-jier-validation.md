# M4 吉尔悬赏与专用对话

固定旧源`6585f407`，仅离线建设，当前实现已构建、计划/状态通过，因果验证进行中；未通过完整任务验收。

## 分层和执行链

- `tasks/bounty_cycle`承接蝎女、六手和吉尔的共同跳跃/揭榜/路线/交付/住宿。
  统一函数更名为`bounty_cycle`，更新所有调用者，不保留旧函数别名或第二个执行器。
- 吉尔固定`requestToRescueTheDuke`，不受蝎女的矿石/凯旋开关影响。
  未传CSC_symbol，因此不改因果。返回要塞后必须实际切王城，不能把起点旅店当终点。
- 先走B4FLabyrinth两个坐标，再独立执行harken退出。harken到城外的完成证据
  来自受控地图子流程和新帧，而不是伪造第三个“仍在地图上”的坐标回执。
- 揭榜、路线退出、交付住宿分别是三个有限正常段。悬赏只交一次，沿用专用休息间隔。

## 专用对话的范围

编译产物保存类型化`DialoguePolicy`；发布时冻结进WvdVision binding与revision。
父子图合并只允许默认/相同策略，冲突直接拒绝。运行中不修改全局配置，不向33字段
profile塞临时开关，也不把吉尔选项加入所有任务的默认列表。

吉尔选项及bondmate关闭图加入同一资源闭包。默认对话必须让位于当前任务已确认的
专用选项；普通地图/战斗/旅店或系统覆盖层仍有反证，不能见字就点。
地图后置、阻塞检测、公共插入和启动恢复消费同一冻结策略，避免只改入口识别。

点击前保存`special_dialogue_pending`，新画面且旧选项消失后才完成回执；
bondmate关闭限定旧ROI`[277,751,330,600]`。点击被拒绝、停止、后置不明时保留意图，
恢复策略禁止自动重发；完成次数不是悬赏交付数。

## 验收断言

首次构建发现ProbeBatch没有范围迭代器，按已有at(index)接口修正后构建通过。
完整状态30方法通过（8.773秒），包含吉尔/蝎女/六手各两轮及专用对话恢复回执。
纯计划8方法通过（34.993秒），11个已接线专项分别核对真实图片闭包，不代表任务执行通过。
构建与验证日志分别为`m4-jier-and-fishing-fix-build2.log`、`m4-jier-state.log`、`m4-jier-plan.log`。

待运行：默认任务不选择吉尔选项、正常场景反证、
专用选项优先级、bondmate ROI、拒绝/停止保留意图，以及35输入完整正常路线。
后续还需路线中插入对话、harken中途遭遇、恢复、连续周期和配置/mod组合。
专项编译或单次正常路线通过不代表上述矩阵全部通过，也不改变58项完整任务分母。
