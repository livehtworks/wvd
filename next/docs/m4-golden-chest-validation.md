# M4 忍洞金箱

固定旧源 `6585f407`，状态/编译与对话分项通过，路线尚待复验，不是完整任务通过。

## 分层与边界

- `tasks/golden_chest` 编译原生有限图，`quests/GoldenChestCycle` 保存阶段/跳跃意图/计数，
  WvdRunState独占生命周期，未引入另一套调度器。
- 第一正常段：ruins/cursedWheel → SSC/Leap（specialRequest后备）→ leap/OK →
  新帧城内 → 实际切王城 → 住宿并领取指定任务。第二段才允许进入忍洞。
- 忍洞只在已知本内画面转向/点击机关，必须识别trapdeactived并退出提示才进入地图路线。
- 路线两个坐标、四个宝箱目标及全部ROI排除区来自旧源码。最后SSC_quit独立退出，
  不把“出口图消失”或“子流程成功”误当六点已完成。
- 每轮开始增加旧开始计数，completed_cycles仅在完整退出新帧后增加；普通续段不重置。
  跳跃已发但结果不明时保留leap_pending，不自动重放。领取意图由FeaturedVisit承接。
- DialoguePolicy新增忍洞的两个有序选项，仍冻结进同一binding/资源revision。
  顺序为dotdotdot、shadow；每次输入前复核当前首选项，不能根据旧候选点下一项。
  与吉尔共用受控对话实现，但默认任务不启用任一专项选项。

## 验证清单

首轮与指定领取同构建：状态/计划40方法通过；私有根`m4-workflow-q1g83xy1`
专用对话3输入Completed，停止/拒绝各1输入且保留跳跃意图。已核对EXE身份与静止。
空宝箱路线在第14次输入失败，mismatch=true，原因同领取夹具漏写400毫秒滑动时长；
不是已跑通金箱。修正夹具后再验，实际四箱仍须另验。

待验：两周期状态回执/正常续段边界，真实manifest闭包，领取子流程，专用对话连续
选择和bondmate关闭，完整路线成功/失败/停止/恢复、配置与mod矩阵。
不得将计划编译或没有宝箱的路线分支当作完整金箱任务通过。
