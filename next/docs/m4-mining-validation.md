# M4 挖矿有限链

## 代码范围

`tasks/mining.*` 编译FFXI-Org的入本、标记寻路、矿点确认、挖取/奖励页、无矿退出，以及
无镐子后的返城、FFXIStone组队和真正住宿。沿用原EOT/RTT，不购买工具、不清包或卖装备。
所有输入仍通过新帧GuardedAction，未知/失败交回既有协调器；本轮仅运行隔离离线设备。

`mining/Progress` 是WvdRunState内的业务子状态，不持设备或图像，不是另一个执行器。
奖励按旧十项顺序取最高分，必须严格大于0.9；ROI保持[4,664,890,283]，无法判断计unknown。
同页重复观察幂等，页面消失后才允许下一奖励；同页分类改变保留冲突，不能加两种计数。
矿点仍为[692,68,140,140]，无矿提示仍为[320,667,423,474]。

补镐子标志跨恢复保留，组队后只进入住宿阶段；实际住宿回执确认前不清标志。
矿物统计与普通战斗/副本次数分开。本有限段预算900秒，节点命中上限明确；不据编译成功
或部分子图通过宣布无限挂机、资源稳定或完整58项任务通过。

## 待验证

- 正式清单绑定四种已实现专项图，其余十一项明确未实现。
- 状态回放、同矿物多次获得、未知分类、未消失奖励页、停止/输入拒绝。
- 实际Maa离线常规/冷启动矿点循环，完整十八次输入的返城组队住宿链。
- 入本/寻路与中途恢复更完整组合、各矿物边界与mod图、当前共享构建回归。

## 首轮记录

`f43ef5c` 构建成功，状态24方法通过（7.654秒）。计划八方法中六通过、两失败：
`mark_auto` 子图没有宝箱专属的 `UnavailableExit`，却被挖矿声明为可转接出口，
因此发布前返回 `COMPILE_EXIT_BINDING_INVALID`，并非已经运行到游戏后的失败。
首轮目录为 `m4-plan-1l5re36a`，日志 `m4-mining-plan.log`（30.901秒）。
修正一只移除不存在的出口绑定，仍保留停止、阻塞和遭遇出口；不放宽编译校验。
实际Maa流程尚待执行，不把纯状态通过写成专项通过。

## 流程首轮与定点修正一

`59502b2` 计划八方法通过（30.472秒，`m4-plan-a61dt_mt`）。挖矿五方法中一通过、
四失败（217.554秒，`m4-workflow-qnrqga1k`，`m4-mining-workflow.log`）。
常规循环实际Completed/五输入，以及奖励页不消失时Failed/一输入/只计一次，已在重建前
独立核对原生产EXE身份。其它分支不据这些局部证据宣布通过。

- 补镐子在第十三次输入拒绝：夹具误写返回键kind=1，真实ClickKey为5；修正夹具，
  保留十八次输入和真实住宿断言，不改产品输入。冷启动closed表示模拟器未运行，
  预期应为RestartInstance/EnsureVpn/StartApplication，不是仅重启应用。
- 冷启动奖励确认出现2052.774ms帧龄，停止负例尚未输入就BUSINESS_CONFIRMATION_STALE，
  连续同矿物确认也触发SCENE_UNCONFIRMED。挖矿专用阻塞探针改为基础四路同步计算、
  原优先级消费，嵌套对话仍串行；保留全部条件、错误传播和两秒TTL。
- 挖矿测试进程看护对齐正式900秒段加收尾，而不是通用90秒；首轮没有把看护超时当PASS。
  以上为首轮原因对应的修正，不抹掉失败或重跑其它已到边界的专项。

## 修正一结果

`228f2f0` 正式产物：五方法/八场景全部通过，274.948秒；日志
`m4-mining-confirm-workflow.log`，私有目录 `m4-workflow-ao80wzzv`。
重建前八场景均独立核对EXE哈希、终态、实际输入数、mismatch=false和真实静止。

| 场景 | 终态/输入 | 已确认业务 |
| --- | --- | --- |
| 矿点常规/模拟器关闭后的冷启动 | 各Completed/5 | fine一次、完成周期一次、普通副本次数零 |
| 先返城组队后住宿补镐子 | Completed/18 | 住宿一次，住宿后才清补给标志 |
| 两页相同矿物 | Completed/7 | fine两次，不能把第二页当第一张旧回执 |
| 未知分类 | Completed/5 | unknown一次，不编造矿物种类 |
| 奖励页不消失 | Failed/1 | fine只一次、周期不完成、后置超时 |
| 首次挖取拒绝/停止 | Failed或UserStopped/各1 | 奖励总数零、周期不完成 |

完整EOT到矿点、中途恢复、各矿物边界/mod与当前全量回归仍待完成，整任务仍PARTIAL。

## 完整入口与分类扩展

正式源码`8651c50`构建、夹具`8071bbc`：三方法两通过、一失败（274.872秒，
`m4-workflow-l07wlk3m`、`m4-mining-entry-rewards.log`）。十种矿物加ROI外干扰共25输入，
每类一次、未知一次；baseline缺失的fine由封存mod提供，保持旧图片优先级。
错位置标记零输入Interrupted。两通过场景均在重建前核对实际EXE身份与静止。

完整EOT已正确执行GCN/EVENT/活动/ZONE2五次输入，进入自动寻路后准备点击mark_auto时，
场景确认帧龄2531.074ms、目标完成2556.924ms，被SCENE_UNCONFIRMED拒绝。
修正一复用基础阻塞探针四路计算，作为blocking_screen的显式parallel_basic参数，
仅挖矿及自动寻路调用启用；不改条件、顺序、阈值或TTL。原挖矿临时专用mode同步退出，
没有保留兼容别名或另一套阻塞规则。完整入本链待复验。
