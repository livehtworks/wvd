# M4 本轮收口与剩余工作

日期：2026-09-15。工作包：`WVD_MaaFramework_M3_Fix_and_M4_Pack_20260914`。

## 范围结论

用户明确不再要求验证全部游戏任务，要求完成当前阶段、commit/push并列明剩余。
本轮公共能力实现及必要定向回归已完成，按此范围收口；不是完整M4通过，也不授权生产切换。
整体保持 `M4_PARTIAL_IMPLEMENTATION`、`release_allowed=false`。
历史完整任务验收 `0/58` 不改写，但不再以全量任务矩阵阻挡本轮交付。

仅推送个人fork的`agent/local-stability-notes`，不向原作者提PR。
未查询或连接安卓设备，未运行VPN/游戏，未打包或更换旧生产入口，停在M5前。
旧Python/config/mod/资产/日志继续保持原职责；`.vscode/`用户文件不纳入提交。

## 实现与分层

- 初始VPN仍通过既有Lifecycle执行器，仅放行明确授权的首段EnsureVpn；正常续段不重复启动。
- 恢复配置与业务状态共享冻结profile，开关不创建授权，崩溃阈值不读取运行中编辑值。
  拒绝非法类型和超出int64的unsigned值，保留既有负数阈值语义。
- 任务文字、上轮耗时由业务状态所有者提供；首轮耗时为空，保留旧结算和跨恢复语义。
- Context提交实际帧身份，Gateway/ExecutionSession绑定RunStore；诊断存储有配额、节流、
  路径/PNG校验和原子写，不建图片池或后台队列，不改变业务首因与输入许可。
- 源码/构建产物/证据索引工具显式选择成员并脱敏；不自动扫描导出原始日志、SDK或用户文件。

正式接线见[业务汇总](m4-business-validation.md)、[PNG诊断](m4-png-diagnostics-validation.md)、
[初始VPN/恢复](m4-cold-start-validation.md)、[语义审计](migration/m4-semantic-audit.md)。

## 构建与验证

| 检查点 | 实际结果 | 边界 |
| --- | --- | --- |
| `5419eff` | 保存本轮实现；首次构建失败 | MSVC字符串/JSON比较错误，未跑旧EXE冒充通过 |
| `a58a585` | 修复比较；下一构建暴露诊断测试缺发布参数 | 生产库构建不等于全目标成功 |
| `007a7d6` | 修复参数，统一构建成功，绑定16个EXE | 源码快照与实际构建记录匹配 |
| 同产物M2 | 104/104，97.511秒 | 核心离线回归 |
| 同产物公共组 | 70方法69通过1失败，80.016秒 | unsigned阈值负例暴露真问题，保留原失败 |
| 同产物VPN/交接 | 15方法13通过2失败，369.720秒 | VPN6/6；交接7/9，两例等待前帧过期 |
| `7fa2957` | 修复阈值，重新统一构建成功 | 最终受测源码检查点 |
| 最终M2 | **104/104，99.871秒** | 退出码0，16产物批次前后哈希一致 |
| 最终定向组 | **48/48，86.529秒** | 状态39、诊断8、冻结恢复配置1方法；退出码0、哈希一致 |
| 审核工具 | 29/29纯Python检查通过，3.719秒 | 源/产物CLI已实际使用，完整审核ZIP端到端未验 |

两次构建失败、阈值修前失败和复杂业务失败均保留，不以最终局部成功覆盖历史。
最后受测代码为`7fa2957`；收口提交只更新文档，不为文档变化重复编译。

私有证据位于`next/.local/`，不提交原始日志和二进制：

- 最终构建：`logs/m4-7fa2957-build.log`；源码快照：`m4-final-source-threshold-fix/source.json`，
  SHA256 `f800bb9e2f688d1ad92bab62d8806085eba83962fd47b727108e4fe3af537b7a`。
- 构建记录：`m4-final-source-threshold-fix/build-record.json`；产物表：
  `m4-final-artifacts-7fa2957/artifacts.json`，
  SHA256 `4ae36d2bccc91df2e52d1dffa94136d6e2e195d20ce765f4b2f31c60a5373030`。
- 最终日志：`logs/m4-7fa2957-final-m2.log`、`logs/m4-7fa2957-final-targeted.log`；
  同产物目录中的`batch-m2.json`、`batch-targeted.json`保存命令、退出码及批次前后身份。

批次前后身份不是每次原生调用的身份替代物。旧M2/data缺少部分逐次EXE记录，
旧state/plan等缺加载DLL记录，不事后用新文件补造历史证据。
最终回归没有重新运行Metadata、资源/性能固定窗口或全部游戏任务。

最终只读复核：定向组56份execution与对应EXE一致、28条SDK记录匹配，
37份快照均真静止且保存成功、与落盘结果一致；三个冻结恢复流程各8输入，
第二代次Completed且无夹具错序，四项非法profile均明确拒绝。
M2本次独立复核限于批次身份与日志，不宣称重新逐例审核。
旧版保护清单最终重算450文件，变化0。

## 已知阻断

1. 共用识别帧龄：某些复杂图超过2秒门禁，产生安全拒绝。交接中的`stop-wait`与
   `wait-budget`在进入等待前就因`LEAP_OBSERVATION_STALE`退出，零输入；
   不是已经验证了等待取消/预算出口。7300秒注入时钟等待和部分保存后转交通过，不能相互覆盖。
2. 部分完整任务路线仍失败，包括既有巨人/暗灯/半自动大恶魔以及部分牛洞、Fordraig等。
   COS完整夹具已补旧EOT步骤但未重跑，不能算通过。台账保留失败和未运行的区别。
3. Metadata句柄/`CLEANUP_PENDING`、`RESOURCE_UNRESOLVED`、`PERFORMANCE_UNRESOLVED`
   仍未闭合。SDK等待、磁盘flush等任意原生阻塞可中断性没有生产证明。
4. 真实NEXT/Pause、用户图片质量、长期挂机、Spark和完整审核ZIP端到端未验。

## 后续优先级

| 顺序 | 工作 | 完成标准 |
| --- | --- | --- |
| 1 | 共用帧龄的限定诊断与修复 | 分解截图/识别/Context克隆/整包校验成本，消除多余工作；保留2秒TTL和错误语义，以少量代表失败链验证 |
| 2 | 设备发现与停止清理所有权 | 查清Metadata差异与CLEANUP_PENDING，证明退出、租约和原生对象释放边界后才开放设备入口 |
| 3 | M5界面和运行控制 | 独立新版完整承接编辑、启动、停止、结果/诊断展示；不提前替换旧入口 |
| 4 | 有限真实验证及生产门槛 | 另行授权设备后验证启动/停止/恢复/VPN、NEXT/Pause和长期资源；随后M6发行、M7整体切换 |
| 按需 | 常用任务组合、审核ZIP、Spark | 按实际用途选择，不恢复冷门任务全量矩阵作为默认前置 |

当前包的有限修正边界已到，不通过反复重跑、延长TTL或调整统计窗口挑选PASS。
后续应定义新的窄诊断范围；不能因安全拒绝而直接关闭门禁，也不能以架构建设为由退役未承接功能。
