# M2 修正与 Gate A

基线：`4932207adc6565ccbac43c4414061eac95712dd5`。本轮不提交、不推送、不切换生产。

## 结论

Gate A：**PASS**。修正后三态识别及运行/门禁共 100 项、M1 23 项、浏览器 4 项通过。
保留原 85 项 M2，增加 15 项针对性用例；没有降低阈值或删除旧断言。
原 `m2-core-validation.md` 是前阶段记录，以本报告作为修正后的证据入口。

## 三项修正

- R01：正常、恢复异常、非法恢复及关键事件满队列均经过统一收尾。按代次只汇总一次，真实静止后才一次性提交终态；存储失败保留主因及独立存储错误。STOP_TIMEOUT 不释放所有权、不被迟到成功覆盖。
- R02：先验证基准坐标，之后只钳制舍入后的原始像素上界；点击、滑动两端及触点移动在计为 accepted 前复核。五种尺寸覆盖四角、邻接点、负数、边界之外及允许区域；实际后端记录验证增量、文本和按键不被缩放。
- R03：应用封存版本化 BehaviorRegistry；运行定义只含实现身份和复制的显式参数。函数指针禁止捕获式生产回调。定义版本为 2，根存储 schema 保持 1；结果记录实际会话代次及绑定。实现版本、动作参数、恢复版本或参数改变均触发幂等冲突。

## 受控验证轮次

第一轮 97/100：关键队列用例没有制造第九条事件；360/450 两个映射夹具采用的 Python 重采样与固定 SDK 不同，在场景识别前失败。
第二轮修正测试前提：有限恢复链制造真实溢出；用固定 SDK 的 Resize 制作合成映射模板。保持 0.99 阈值及坐标断言，100/100 通过。
两轮构建均先正常退出再运行测试。完整日志、运行数据与此处冻结差异保留在私有证据目录 `next/.local/m2fix-m3-1ce5ae7f95994ff19cf4c78042336e2c/`。

Gate A 只放行本工作包的 M3，不证明生产可用。`RESOURCE_UNRESOLVED`、真实 NEXT/Pause 样本和底层阻塞取消边界继续保留。

M3 接入及 Android 前台查询修正后的最终产物已再次通过同一 100 项 M2、23 项 M1 和 4 个浏览器场景；
原生 EXE/hash、最终逐例结果与中间快照完整性边界，统一见 [M3 阶段报告](m3-device-vision-validation.md)。
