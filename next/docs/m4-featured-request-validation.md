# M4 指定任务领取

固定旧源 `6585f407` 的 StateAcceptRequest 和 SSC 专用 stepThree，仅离线实现。
首轮构建通过，状态/计划合计40方法通过。领取流程首轮失败，正在修正夹具；
不算牛洞、忍洞完整任务通过。

## 原行为与新落点

- `tasks/featured_request` 先完成真实住宿，再走 guild/guildRequest/guildFeatured。
  牛洞三次 `[150,1000,150,200]`，忍洞一次 `[150,1300,150,200]`；细滚动均为
  `[150,200,150,250]`。不在未知页面滚动。
- 点击偏移分别 `[266,257]`、`[300,150]`，输入前重识别，不保存滚动前坐标。
- 牛洞已领取状态的旧ROI是 `[0,posY-200,900,posY+200]`，第四项为高度。
  新纯视觉明确裁剪超出底部的部分，负起点返回Error；不误写成400像素高。
  忍洞旧流程没有此检查，不能据牛洞状态跳过忍洞选择。
- `quests/FeaturedVisit` 由WvdRunState唯一持有，记录访问、选择意图和回城回执。
  每次访问重置住宿回执；同一次恢复不重置、不再次付款。选择拒绝、停止或未知
  后置保留pending，自动恢复不重发。摘要的selections_confirmed不是奖励到账数。

## 待验断言

首轮证据：`m4-featured-golden-state-plan.log`，40方法/46.426秒；
`m4-featured-golden-workflow.log`，12方法/411.216秒，3失败。
私有根 `m4-workflow-q1g83xy1` 的牛洞和选择拒绝例均在第9次输入失败，
原因是夹具漏写滑动duration，默认按0校验，但正式输入为400毫秒。
未修改生产时长、门禁或阈值；夹具现明确断言400。已独立复核失败产物身份和静止，
保留mismatch=true，不能算安全拒绝用例通过。

修正一在`m4-workflow-4o72cdln`中三方法/七场景通过，重建前已核对EXE身份、
终态、输入数和真正静止。普通牛洞13、已领取12、已领取标记在ROI外13、忍洞11输入；
选择拒绝/停止各12输入、pending保留；未知入口0输入Interrupted。
此证据不等于牛洞或忍洞整体任务通过，完整周期与恢复矩阵仍分别记录。

状态：两次访问分别住宿，已领取访问不增加选择数；待处理意图跨Recovery保留，
重复回执幂等，pending禁止访问完成。

真实Maa：两种滚动/偏移，已领取在ROI内外，拒绝/停止选择，未知页零输入。
源码消费者已接入牛洞和忍洞有限任务，整体成功及恢复/配置/mod矩阵仍待验证。
