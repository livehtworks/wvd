# 蝎女实机续段：交赏、住宿与下一轮

## 现场顺序

1. 上一轮 B2F 战斗后返回王城；公会悬赏页只有一处发光的“達成報告”。人工点击一次后出现“獲得報酬 / 已領取報酬”，关闭回执后该报告消失。没有点击其它悬赏或“獎勵確認”。
2. 旅店选择标准房，确认页明确显示 200G；只确认一次。住宿动画显示背包补充和瘴气清除，回到旅店后金币由 73,560,274G 变为 73,560,074G，队伍状态恢复，然后离开旅店回王城。
3. 原生 `Scorpionesses` 根任务从王城启动，矿石跳轮按钮被接受，游戏回到王城；根任务在 `navigation.time_leap_without_causality.budget_exhausted` 失败。现场 `city.royal.identity` 命中，跳轮按钮与轮盘标题均不命中。复查诅咒之轮显示豪雪地带的“美麗礦石的真相”为当前紫色项；没有再次跳跃。原因是点击后的过渡帧先走回 `LeapRoute`，而旧路由不再检查完成态。已修正该路由，但尚未用下一次自然跳轮复验。
4. 从现有王城继续，作者流程 `guild-open-bounty-page` 真实运行 `Completed`，显示新的“雙壁之蝎女”悬赏弹窗；关闭弹窗后列表可见。弹窗只是展示刷新结果，不是领取操作。
5. 返回王城，作者流程 `city-to-abyss-b2f` 真实运行 `Completed`，落在 B2F 圣堂入口。最初直接试点 `(505,760)`、`(506,821)` 时漏做旧路线规定的两次拖图，因而把第二点触发哈肯菜单误记为路线错误；该次试点不能证明原路线失败。
6. 从 B2F 哈肯起点按旧代码先拖图 `(100,1200)->(700,250)`、选 `(505,760)`，固定 `(136,1431)` 点击后地图与浮层没有变化；点击目标格上方“自動移動”浮层约 `(520,636)` 后实际进入蝎女战斗。战斗结束后，在地图选 `(506,821)` 并点击浮层约 `(520,718)`，实际回到哈肯并打开楼层菜单。选择“什麼都不做”留在 B2F。截图见本机 `next/.local/scorpion-old-*.png`。这证明两个坐标分别是去蝎女房间与返哈肯的顺序点，而不是两个任意屏幕点击。

## 本轮代码与边界

- 繁中“懸賞令”页签 ROI 扩至包含文字左缘；保存真实“達成報告”、已领取回执和关闭按钮模板。正式探针在提交前列表命中报告，提交后列表和领取回执不命中报告；回执及其关闭按钮在回执帧命中。
- 繁中提交子流程改为“报告 → 回执 → 关闭 → 记录一次完成 → 再查报告”，只依据发光的报告，不依据悬赏名称。提交后没有回执则保留待确认状态，不盲目重试。此自动子流程尚未在新的可交报告上实跑。
- `REST_INTERVEL=1` 在悬赏任务中解释为每次出战后住宿；本轮 200G 住宿是人工确认，不冒充自动住宿验收。
- 完整蝎女根任务没有通过：跳轮返回路由只完成代码修正；地图导航已针对坐标目标改为在所选格附近识别并点击“自動移動”浮层，末点的哈肯楼层菜单也已接入子图、父任务点确认和返城前确认。真实菜单帧对现有繁中组合探针为 Hit，选格地图帧为 NoHit；两个点的浮层旧模板在局部 ROI 分别取得约 0.46/0.37 分，未选中地图帧为约 0.20/0.16 分。原生定向检查与 Release 构建通过，但新候选自动地图流程尚未从圣堂起点实跑。战斗这次由游戏内自动战斗完成，不是配置的“法术+地裂”原生技能策略验收。不得把人工路线或分段作者流程成功记作完整任务成功，也不要从住宿或跳轮重新跑本轮已经发生的副作用。

证据图保存在 `next/.local/scorpion-*.png`，服务运行记录位于 `%LOCALAPPDATA%/WvdNext/runs`。这些本地证据不随资源包发布。

## 2026-09-26 冷启动续跑

- 上轮从王城启动的原生任务已自动识别并提交公会悬赏页的发光“達成報告”，随后进入旅店标准房的 200G 确认页；确认支付和离开旅店由人工完成。再次从王城启动后，任务完成跳轮并抵达公会悬赏页，但被“新的懸賞令”展示弹窗挡住。此弹窗不是领取悬赏。
- 候选现已加入繁中住宿“確定”模板和悬赏展示弹窗“關閉”模板；Reveal 退出路径会有界点击关闭展示卡。Release 构建及打包完成。新的自动住宿确认和自动关闭展示卡仍未在实机验证。
- 按用户要求，在配置中将 `AUTO_START_CLASH` 设为 `false`，从关闭状态连接 MuMu 设备 `127.0.0.1:16448`，未启动 Clash/VPN。原生任务成功唤起游戏，到标题页后游戏提示“發生錯誤，請確認網路環境後再試一次”。任务以 `OBSERVATION_PHASE_TIMEOUT:wvd.boot` 结束；手动点游戏自带“重試”一次，20 秒后仍为同一网络错误。未进入王城，未跳轮、领取、交赏、住宿或战斗。本轮完整任务受直连网络阻断，不得记为通过，也不应以反复重启模拟器代替连通性解决。
- 本机截图 `next/.local/direct-run-after20.png` 与 `next/.local/direct-retry20.png`；运行记录在 `%LOCALAPPDATA%/WvdNext/runs`。任务已停，模拟器保留在错误页供现场核对。

## 2026-09-26 繁中根链实跑完成

- 用户改用另一 VPN，工具保持 `AUTO_START_CLASH=false`，全程未启动 Clash。曾在哈肯过渡时遇到安卓实例掉线；只拉起同一 MuMu 实例，游戏恢复后确认实际在 B2F 哈肯，而非王城。一次从错误起点启动的任务零输入停在公会入口，主动停止后人工回王城，再按正式入口继续；该次不计入成功验收。
- 正式任务从王城反复推进时先后定位三处缺口：技能弹窗仍使用英文 `spellskill/skillDetail` / `OK`；战后恢复仍使用英文 `trait` / `recover`；最后地图点已确认到达哈肯后，地下城调度没有把楼层菜单作为终点。分别接入实帧繁中模板、限定 ROI 和语义映射，并将角色/恢复面板纳入 Pause、未知卡死和普通对话的排除检查。哈肯只在最后一点进度为 `2/2` 且楼层菜单可见时结束当前地下城段，通用提示处理将稳定楼层菜单交还调用方。
- 旅店付款后存在普通剧情箭头和“背包已補充完畢”中间页；付费后仅推进这些页面并退出旅店，不重新购买。繁中“達成報告”只提交可见发光项，关闭回执后重新检查列表，不把悬赏刷新弹窗当作领取。
- 最终从王城启动的正式 `Scorpionesses` 根运行在 `%LOCALAPPDATA%/WvdNext/runs/5AAA4527-136B-466A-876F-585CDC456EF5/1` 落盘 `Completed`：旧报告提交、200G 住宿、矿石跳轮、悬赏刷新、B2F 双点导航、蝎女技能战斗、战后整体恢复、哈肯返王城、提交本轮报告和间隔住宿连续完成。业务快照为 `completed_cycles=1`、`reports_remaining=0`、`task_step=2`、`inn_rest_completed=true`；3 个业务段完成，62 次输入执行、0 次拒绝。最终王城截图在本机 `next/.local/scorpion-root-completed.png`。
- 本轮证明繁中蝎女普通路线的一次真实循环，不证明六手路线、随机宝箱/陷阱、其它任务、工作台 UI 启动、异常恢复或长期挂机稳定性。旧英文素材继续服务英文界面；其它尚无繁中实帧的通用提示不能仅凭名称批量改映射。
