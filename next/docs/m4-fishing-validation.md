# M4 钓鱼接续验证

固定旧源 `6585f407` 的 fishing/fishing2 共用case；本阶段仅离线实现，未开放设备。
基础产物与转向、未知页已有分项证据；补饵接续正在验证，不能写成两项完整任务通过。

## 分层与语义

- `tasks/fishing` 编译抛竿、收获页和一次有限钓鱼图；Maa拥有执行与等待。
- `vision` 负责同ROI的0/8鱼饵分数比较、尺寸/鱼类分类，以及固定水域浮标检测。
  抛竿前不是单看nobait模板；先超过原阈值，再严格大于8bait的分数。
- `fishing::Progress`由WvdRunState持有，保存待关闭收获、分类、完成次数、
  抛竿计时和超时失败数；不保存截图、坐标或SDK句柄。
- 右扫五次、左扫两次均100ms，近端抛竿4000ms、远端2250ms。
  每次重新校验页面、停止及门禁；保持地图默认400ms，不统一修改所有滑动。
- 正式单轮图在抛竿后记录等待起点，再等待10秒。等待浮标每次1秒，消失则100ms拉杆，
  后等3秒；严格超过300秒才允许超时收竿，确认离开striking后登记失败。
  两组等待节点交替，保持既有每节点256次上限，不在256秒提前报超时。
- 旧Farm先裁剪 `[250,500,400,600]` 再检测浮标；新专用模式同样裁剪，
  返回存在性，局部坐标不被当成全屏输入位置。保留原M3独立bobber模式契约。
- 鱼类只在 `[0,1100,900,150]` 查找，尺寸仍全屏。各自取最高分且严格大于0.9；
  未知鱼类记录“未收录”，未知尺寸仍计总鱼数但不伪造分类。
- 新帧分类产生待确认回执，关闭页后新帧确认才计数。相同回执幂等，停止、关闭失败
  和恢复保留未确认状态；不能从历史文件自动复发物品操作。
- 抛竿前另记待确认意图，咬钩页确认后才重置计时；重复抛竿不会沿用上一竿的超时。
  收获/抛竿未确认时应用恢复策略不重启来重放，缺饵也不是重启理由。

## 当前验收与剩余

新增断言：两种抛竿精确时长、空饵/未知零输入、停止/拒绝、页面改变；
分类正例/ROI外/未知尺寸、关闭失败与停止零计数；完整一次抛竿拉杆收获；
25分类状态回执、恢复、旧代次拒绝、300秒严格边界和非法时长。
`3a26482` 构建通过，状态29方法通过（8.493秒）。
同产物M2全组104方法通过（94.991秒，`m4-fishing-m2.log`），不代表未测专项通过。
`m4-fishing-workflow.log` 首轮9方法5通过4失败（289.080秒），私有根
`next/.local/m4-workflow-tuq6fexm`。重建前已核对九个实际场景的EXE身份、输入和静止：
近/远抛竿各8输入，场景变化失败1输入，三种分类各1输入，缺回执0输入，
收获页不消失失败1输入，以及完整单鱼10输入。

四个失败方法包含三个夹具错误，不改写为已通过：
1. 缺饵的具体原因在 `sessions[-1].reason`，Run总原因是 `RECOVERY_REQUIRED`；
   初轮安全中断且零输入，但断言错读了字段，后续未知例未执行。
2. 拒绝输入必须在预期command中设置 `reject=true`；不存在 `reject_after_first`
   这个夹具开关。抛竿测试多出第二次输入并被因果后端拒绝；收获测试实际关闭成功，
   不能把它算拒绝场景。两个方法中的停止分支因此尚未执行。
3. 旧资源语义是基础图片优先于mod；首轮的0/8同分例仍用了基础8bait。
   修正夹具只从隔离源移除8bait，再提供mod并断言实际来源，不改正式资源优先级。

修正一保留这些原始结果并使用已有夹具契约；另补地下城转向、90秒未知页窗口和
普通覆盖层返回的实际接线。未知页计时单独属于本识别Session，不替代本内十帧冻结。

修正一 `6517f6d`：状态29方法通过，流程12方法全部通过（429.322秒），
私有根 `next/.local/m4-workflow-p777ic33`、日志 `m4-fishing-correction1-workflow.log`。
20场景在下一次构建前逐项核对实际EXE身份、根终态、输入数和真正静止。
包含近/远8次输入、单鱼10次、三种分类、停止/拒绝/场景改变、真实mod同分、
90秒未知等待零输入，以及地下城定位2输入、已钓鱼/未知定位零输入。
此轮不是完整补饵，也不是实际300秒浮标等待通过。

## 补饵接续

`tasks/fishing_supply`复用既有地图、入本和单鱼子图，正式编译入口绑定两个TaskID。
有限Session分别完成一次收获或“转交后回到旅店”“重新进入钓点”；同一Run状态
贯穿正常续段。尚未通过下面测试时不注册完整任务通过。

- 缺饵确认后才去818/928、打开物品页、找鱼饵并进入接收者页。
- 70笔转交分别记录意图、新帧回执；计数是`transfer_inputs_confirmed`，
  不冒充OCR读取的物品数量。被拒绝或停止后的待确认意图禁止恢复自动重放。
- 转交结束必须确认旅店；下一段DH/DH-R6、339/555、重新开钓，
  确认cast且缺饵谓词为假后才增加`refill_trips_completed`。仍缺饵则有界中断。
- 仅明确城内/已完成路线的页面允许旧固定物品点。未知页不点1/1找出路。
- 状态测试拟覆盖两轮70笔、幂等、旧代次拒绝和错误顺序；因果流程拟覆盖
  101次输入的补饵/返钓/一鱼三段、第一笔转交被拒绝、未知入口零输入。

补饵首次构建通过；状态30方法通过（8.761秒）。纯编译8方法7通过1失败，
真实清单指出`ItemList.png`/`return.png`缺失。旧Windows文件实际为`itemList.png`，
而FindCoords助手将字符串`return`解释为Android返回键，不是模板。
据此修正正式补饵编译器与因果测试，未增加伪造资源、未削弱缺图断言；
首轮日志`m4-fishing-supply-plan.log`保留，修正后需重建复验。

资源修正后8个计划方法通过（34.320秒）。随后首轮补饵流程3方法均报告夹具字段错误，
日志`m4-fishing-supply-workflow.log`，根`next/.local/m4-workflow-rho2mp65`。
结果文件独立核对：未知入口Interrupted/0输入；完整例和拒绝例均Failed/7输入，
原因`TARGET_REQUIRES_CONFIRMATION`，均未开始转交。三份实际产物身份/静止已核对。
根因是OpenInventory把business条件混入fixed_click的目标证明，正确被门禁拒绝。
修正为先observe业务阶段，再对新帧视觉执行fixed_click；不改变business或低置信目标的授权。
夹具同步改用已有`state`/`backend_calls`字段并检查mismatch，不能把这次失败说成转交拒绝通过。

修正一五方法三通过两失败，日志`m4-fishing-supply-correction1-workflow.log`，
私有根`m4-workflow-o6qb7dor`，五份实际EXE/终态/输入/静止已在重建前核对。
真实300秒浮标等待Completed/1输入/失败抛竿数1；转交拒绝Failed/11输入、pending保留；
未知入口Interrupted/0输入。全链Failed/80输入，70笔已确认、phase=3、pending=false，
尚未回到旅店或钓点。返回前场景识别2047ms，被2秒帧龄门禁正确拒绝。
修正二将返回页按box/recipient/transfer/icon/itemList分别识别，不放宽TTL。

下载插入首轮Failed/1输入，后置从frame.captured到识别Hit为2524.701ms，
随后SCENE_UNCONFIRMED。对应修正先识别正常游戏后置，再查其它已知覆盖层；
后置只判断已知页，输入场景的覆盖层优先级不变。

修正二完整补饵一方法通过（1631.369秒），日志
`m4-fishing-supply-correction2-full.log`，私有根`m4-workflow-x9yq7v2w`。
实际根终态Completed，共101次输入，三段分别81/10/10次输入，全部真正静止且结果已保存。
确认70笔转交、一次补饵返钓、最终phase=0、收获一条大三文鱼；重建前已核对EXE身份。
同阶段下载回钓鱼分项在`m4-workflow-q1g83xy1/fishing-round-download`通过，
11次输入，无夹具命令不匹配。两份记录属于当时构建，不能冒充后续新构建的回归。

仍需重复抛竿/连续轮次、其它普通覆盖层和中途恢复配置矩阵。
实际300秒浮标等待已有上面的分项通过证据，不代表全部超时分支通过。
未把有限抛竿或一鱼结果注册成完整fishing任务；任务分母仍为58，放行仍为false。
