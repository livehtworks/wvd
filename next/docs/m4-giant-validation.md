# M4 巨人专项有限循环

## 源码与分层

冻结旧 `QuestFarm` 的 `gaintKiller` case（4045行）：局部EOT选择要塞七层，固定点
560/982，再到harken2，返回旅店，按 `(开始次数-1) % (REST_INTERVEL+1)` 决定住宿。
原任务已经不再判断巨人位置，默认击杀灯怪，不能恢复已停用分支；拼写保留gaintKiller。

`tasks/giant.cpp` 编译有限Maa图，复用入本、路线、遭遇、回城控制和有回执的住宿；
`WvdTaskPlan::with_entry/with_route` 只改变局部编译参数，目录原树仍只读可追溯。
WvdRunState独占开始/路线/完成事实，通用运行器不理解巨人或旅店。

开始次数保留旧计数口径，完整完成数另记。已开始周期恢复不重计，已完成路线不重走；
确认付款后的退出恢复不重付。待确认付款不重启重放；路线未完成、住宿应做未做不能完成。
ACTIVE_REST关闭不取消该case的专用住宿条件；负间隔明确拒绝，不静默套普通副本规则。

回城每次输入后先检查Inn，避免旧fallback整串盲点入城后的角色。只操作当前已识别出口
或关闭已确认地图，不在未知页面盲点1/1；未知/不收敛由有限预算返回恢复，不能伪造到城。

## 验收状态

实现已构建，流程尚未通过，不计完整任务通过：

- 两点、回城、真实住宿全链与冷启动；预期EOT名称来自只读固定Git的AST。
- 两个有限普通周期，REST_INTERVEL=1且ACTIVE_REST=false，只首轮住宿。
- 第一输入停止/拒绝，保留一次开始、零完成；状态测试检查提前完成拒绝及恢复幂等。

正式源码 `fbc6196` 已构建，状态全组22方法通过，含巨人提前路线/住宿完成拒绝、
恢复保留路线、重复回执幂等以及下一轮专用休息间隔。
流程首轮三方法8.722秒，全部在发布前返回
`COMPILE_IMAGE_NOT_IN_MANIFEST:image/returntotown.png`，零连接、零输入。
日志 `m4-giant-workflow.log`，私有根 `m4-workflow-h1tth04c`；各方法首场景失败后未执行
其后场景。原因是Windows合成资源同时写returntoTown与returntotown，只保留一个文件名。
修正一令测试按正式包既有别名生成规范资源，不改生产资源校验；实际流程仍待复验。
修正一复验三方法24.563秒失败，私有根 `m4-workflow-g4o9xi44`，日志
`m4-giant-workflow-correction1.log`：资源发布已通过，开始确认返回
`BUSINESS_CONFIRMATION_STALE`，零输入。首场景确认新帧耗时2290.603ms，超过2000ms。
这是另一项真实业务识别成本问题，不是别名修正无效。开始条件拆成五个具体场景分支，
仍覆盖原条件并集、排除全部阻塞层并共享同一回执；不延长TTL，待定点复验。
开始条件修正一复验仍失败：三方法25.288秒，根 `m4-workflow-f6b702fm`，日志
`m4-giant-entry-correction1.log`，确认帧2621.745ms，零输入。
进一步核对发现外层all的二路分片将blocking_screen纳入，后者又进入默认对话四路扫描；
固定OpenCV在嵌套parallel_for中将内层串行。修正二从外层并行白名单移除会间接进入
对话分片的三个模式，保留条件顺序、全部Error传播和独立帧校验；需要共用视觉回归。
修正二复验三方法87.502秒仍失败，根 `m4-workflow-9o5fuf7y`，日志
`m4-giant-entry-correction2.log`。正常/输入失败场景仍在开始确认前被帧龄拒绝；两周期
场景已开始并输入八次，随后 `SCENE_UNCONFIRMED`，没有错误坐标输入。其后冷启动/停止
子场景因先前断言失败未运行。已达到这项开始确认问题的有限修正边界，保留阻断，不再
重复运行该组挑通过。正式源码 `720b6db` 共用M3非Metadata九方法100.032秒通过，
根 `m3-fixes-ackedjyx`，日志 `m4-shared-vision-m3.log`；不能据此宣布巨人业务通过。

嵌套并行行为已核对固定 [OpenCV 4.12.0 parallel.cpp](https://github.com/opencv/opencv/blob/4.12.0/modules/core/src/parallel.cpp#L484-L505)，
但它不是本轮剩余识别成本的完整归因，性能未决项继续保留。
离线检查器另新增 compile_specials_manifest，复核已实现的陷阱/巨人资源与图，
同时列出其余十三项；不建Run、不提供执行入口。此编译专项及负间隔测试待下一次构建。

专项输入因果、失败后恢复位置、各配置边界与全部任务矩阵仍需实测，不以此文档算验收。
仅离线专属后端；没有连接游戏、购买资源或改变旧配置。
