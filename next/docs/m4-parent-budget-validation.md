# M4 父流程有限预算

完整 quick 同例实际耗时 284.103 秒；子图为完整 quick+普通退路保留 900 秒，普通图 600 秒。但原路线仍只有 400 秒、迭代 760 秒，会让子链单独可用而接入父任务后提前超时。

当前由编译期定义组合预算：路线为开箱预算加原路线 400 秒余量，正常迭代再加原入本/补给 360 秒余量。普通/quick 路线分别 1000/1300 秒，迭代 1360/1660 秒；加 120 秒启动恢复后最多 1780 秒，仍小于 30 分钟硬上限。

这是有限 Session 的总预算，不把它称为旧版无进展检测窗口。父段时间不会在子任务返回时重启，单节点次数、输入帧 TTL、停止预算均不改变。多个连续遭遇仍可能耗尽总预算，正常续段与恢复原因需在完整任务验收中核对。

对应测试已通过，核对普通/quick、路线/迭代四种正式运行定义的预算及零输入到点；与死亡提示补验合计 6 方法/16 场景通过（329.929 秒），证据根 `m4-workflow-wsi2pj6e`。原短预算统计断言按这次明确的组合定义更新，不修改业务动作、计数和终态断言。完整路线长时运行仍属于逐任务验收，不把这四个到点场景冒充挂机通过。
