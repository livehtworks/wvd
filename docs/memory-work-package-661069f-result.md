# 661069f 内存与回归工作包交付

基线 `661069f5688253270d1b940e084ce19ceca01373`。本轮只修改 `next/native`、现有定向原生测试及事实文档；未动旧 `src/`、`config.json`、mod、用户日志和 `.vscode`，未自动 commit/push。源码、EXE、前端与资源的最终身份见候选 `next/dist/wvd-next-native/DELIVERY_STATUS.json`。

| 项目 | 当前结果 | 仍未证明 |
| --- | --- | --- |
| M01 | Application 共享匹配预算；通用模板、WVD、portrait 和浮标的叶级大计算均受可取消 RAII 准入。已知双积分图、结果图、DFT 余量及灰度/遮罩/排除/缩放计入估算；合法超目标单项独占。 | 256 MiB 是调度目标，不是实际峰值或安全线；OpenCV 隐含分配仍须在自然现场观察。 |
| M02 | Service 共享解码资产缓存；同规范素材单次装载，128 MiB LRU 目标；模板/遮罩与不可淘汰的时序状态分离。mask 键只含影响像素的原图、缩放位模式、裁剪和亮度阈值。结果 JSON 另有限量。 | 静态 539 张 PNG 合计仅是理论 BGR8 载荷，非会话热集。仍在借用的像素可使留存暂时超过目标；租约最后一次释放时立即复核淘汰。 |
| M03 | 候选用途明确区分有序首命中和全量求值；前者按原优先级逐项安排，后者保留全部子项；并行批中已发生的资源故障优先返回。 | 未在本轮真实游戏帧上覆盖 NEXT/Pause 或所有对话反证。 |
| M04 | Windows 平台层固定槽、预开日志句柄、进程/系统提交采样；匹配/解码/预处理前记录，OOM catch 不再构造 JSON。会话末写 `recognition.resources`。 | 未自然遇到 OOM，故低内存现场终态完整性未实测；旧故障调用栈仍不明。 |
| F01 | 成功 Return 只查当前子帧 pending，父回执保留；定向测试验证事件返回后由新帧结清。 | 尚无本轮实机嵌套事件。 |
| F02 | 繁中 `worldmapflag` 的默认只读观察接受 `worldmap.open` 联合证据；显式叶级覆盖和定位仍拒绝。隔离资源包定向测试已完成编译与发布，校验两张联合证据图均进入发布身份。 | 尚无本轮真实地图观察。 |
| F03 | 英文 Report 恢复原有准备/点击/后置确认/计数；繁中缺提交配方时完整任务准备期拒绝，独立 Reveal/开页不受影响。 | 英文 Report 源码链未实机提交；繁中仍缺可信提交素材，本轮未提交悬赏或消耗资源。 |

验证仅包括 Release 增量构建、`test_native_author`、`test_native_flow`、`test_native_recognition` 三个定向可执行文件，以及静态 PNG 头清单。未跑全量测试、旧 `validate.py`、长内存矩阵或设备任务。**构建通过不等于实机或资源稳定性通过。**旧事故与新治理严格分栏见 [内存报告](opencv-memory-review-20260925.md)。
