# 项目当前事实

## 当前架构迁移（2026-09-24）

- 用户已确定 WVD Next 移除 MaaFramework，目标工作包为 `WVD-NATIVE-RUNTIME-376ca78-20260924`。当前仍在实施，**没有完成正式入口切换，也没有生成可交付的无 Maa 产品**。
- 已在独立源码中建立中立 FlowProgram/FlowExecutor、直接 OpenCV/ORT 识别、MuMu 截图 helper、ADB 和 scrcpy 控制通道，以及 WVD 业务操作和单会话端口的初步实现。它们只通过隔离编译及有限机制验证；旧 `Application`、`RunCoordinator`、作者流程编译和正式 CMake 仍依赖 Maa。
- 旧版交付与用户配置、流程、mod、日志均保持原样。此阶段未连接模拟器、未操作游戏；完整范围及去向见 `next/docs/native-migration-map.json`。

## 当前产品

- 旧版 Python/Tkinter 程序仍保留，未覆盖 `src/`、旧 `config.json`、`mod`、日志或 `dist/wvd`。
- 新版 Windows 可操作程序位于 `next/`，技术底座为 C++20、MaaFramework 5.13.0、OpenCV 4.12.0、Boost.Beast 与 Vue 3。
- 新版唯一服务入口为 `next/native/app/main.cpp`，装配层为 `next/native/app/application.*`；设备、运行、配置和流程数据均由后端持有，Vue 不自行模拟状态。
- 可双击交付目录为 `next/dist/wvd-next`，入口为 `启动WVD新版.bat`。新版用户数据保存在 `%LOCALAPPDATA%\WvdNext`。

## 已接通能力

> **阻断状态（2026-09-23）：** WVD Next 已在既定 MuMu 上真实完成繁中启动链，以及蝎女任务的“王城 -> 荒屋 -> 诅咒之轮 -> 美魔矿石的真相 -> 跳跃 -> 返回王城”前段。公会领取、入本、战斗和交任务仍未跑通，完整任务链为 `NOT_DELIVERABLE`。启动链事故根因和早期证据见 `next/docs/runtime-chain-incident-audit-20260922.md` 与 `next/docs/runtime-chain-repair-local-closure-20260922.md`。

- 工作台可编辑并保存模拟器、目标、探索、战斗、战斗方案、任务点、日常和高级设置；战斗方案通过左侧可搜索列表切换，右侧只编辑当前方案；旧配置只读导入新版副本。
- 明确选定 MuMu 路径、ADB 地址和实例编号后，可从界面连接、断开、截图并显示异步失败原因；截图与任务执行共用设备所有权。
- 可直接运行现有任务，启动定义读取所选任务的有效配置；生命周期恢复继续承接游戏启动、重连和已启用的 Clash/VPN。
- 流程编辑器支持新建、复制、保存、删除、任务转编辑副本、节点参数、连接语义、撤销/重做、选中节点调试、已保存版本运行和停止。
- 作者流程已接入识别、受控点击/滑动/返回键、可中断等待、有限重复、失败出口、战斗/开箱/业务确认和现有任务阶段。
- 试识别支持当前帧和 900x1600 本地 PNG；模板、OCR 与 WVD 既有识别模式返回 `Hit / NoHit / Error`，不触发设备输入。
- 运行结果显示真实 Run 状态、当前编辑节点、耗时、输入统计、失败原因和有界诊断截图。

## 本轮实操事实

### 额外流程事件整改（2026-09-23，未提交）

- 作者事件规则、公共处理流程引用、发布期 Maa 候选、Custom 检查点、InputGate 父回执挂起/新帧续接、外部阻断终态和界面编辑已进入当前工作树；隔离 HTTP/UI 保存与若干固定 Maa 合成场景通过。
- 当前结论为 `PARTIAL_NOT_DELIVERABLE`：原生子图检查点尚无作者事件作用域，导航遇怪、开箱/战斗计数、`task_stage` 原生 Dispatch 所有权及正式 `Application` 装配运行未按本包完成验收。未晋升发行目录；该工作包本身未操作真实设备。逐项证据与剩余工作见 [flow-events-local-closure-20260923.md](../next/docs/flow-events-local-closure-20260923.md)。
- 后续单独授权的实机采样已在配置的 MuMu 实例 2 完成王城、世界地图、公会、委托和悬赏列表/蝎女详情的原始截图采集，私有索引在 `next/.local/real-scene-20260923/README.md`。跳轮后进入“懸賞令”会一次性刷新全部悬赏；本轮出现的“新的懸賞令”告知页不是必经状态，也不能代表全部悬赏，刷新前列表未采。用户确认悬赏无需领取：跳轮、切换悬赏页、讨伐、回列表逐项提交；未讨伐过悬赏目标时告知页可能不出现；双目标首次提交后另一项上移，再提交一次。普通委托任务才有接案与提交。本轮未提交悬赏、领赏或执行自动任务；游戏留在王城。本次没有运行测试或构建，也未把样本合入正式资源。
- 第二次前台实跑完成“王城 → 荒屋 → 美魔矿石的真相跳跃 → 王城 → 公会 → 委托清单 → 悬赏令列表 → 公会 → 王城”；悬赏页直接显示列表，未出现刷新告知页。最终停在王城。此结论只证明人工逐步操作的真实页面路径，不等于新版自动流程通过；未接案、提交、领奖或讨伐。
- 随后的新版作者流程 `live-bounty-refresh-return-20260923` 已通过正式运行入口在实例 2 自动走通同一无告知页路径，第二轮 `Completed`，9 次底层输入，最终回王城；私有证据与第一轮失败见 `next/.local/real-scene-20260923/README.md`。第一轮暴露 `guild_icon` 匹配框偏上，点击其中心没有打开公会；第二轮在王城身份确认下使用实测入口中心固定点完成。现已将王城可见八处建筑入口统一接入不含背景的图标形状模板，并在已采王城/公会/悬赏/地图帧上核对位置与负样本；新模板尚未在正式运行入口重跑，当前交付目录也未替换。告知页出现时的自动分支和完整蝎女任务仍未验收。

### 可组合流程整改（2026-09-23）

- 作者模型已增加 `call`、`slot`、`route`，公共步骤、流程块和任务共用现有 WorkflowRepository、作者编译器、Maa Pipeline 与 RunCoordinator；没有新增第二套执行器。
- 公开参数使用声明式标量契约和节点字段绑定；语义素材通过 `semantic-assets.json` 按用途和游戏语言解析。作者定义、语义目录及图片哈希已进入运行包 revision。
- 12 份公共定义已导入编辑器并达到 `STRUCTURE_VERIFIED`。所有既有游戏任务仍为 `LEGACY_NATIVE`，本轮没有切换真实任务入口，也没有连接 MuMu、ADB 或操作游戏。
- 隔离 HTTP 编辑链已验证：创建公共步骤、以不同参数调用两次、插槽扩展、保存重开、展开定义、返回原调用节点以及拒绝删除被引用定义。产品页面控制台无错误。
- 当前仍缺可靠繁中 `guild.commissions.page`、`guild.bounties.page`、`guild.bounty.first_item`、`guild.bounty.receipt`；普通委托接案、悬赏提交及依赖这些资源的组合保持阻断。
- 当前交付 `automationd.exe` SHA256 为 `67C622B521416B1ED311431A0C0584F661AF25C13A1A567674A67C67F20EEEBE`，交付目录为 `next/dist/wvd-next`。隔离服务检查后端口已释放，`automationd.exe` 进程数为 0。

- 繁中蝎女实机链已真实完成“王城 -> 荒屋 -> 诅咒之轮 -> 美魔矿石的真相 -> 跳跃 -> 返回王城”；最后一层曾因只接受英文城市锚点而把成功跳跃误报为 `SCENE_NOT_FOUND`。因果轮列表现按“目标尚未出现”向下滑，不再以始终可见的跳跃按钮作为是否滚动的条件。
- 城市入口现统一使用八张图形模板：旅店、寺院、打铁铺、荒屋、公会、矿石商、道具店、郊外。旧宽图包含建筑背景/状态亮点，公会入口曾误定位到图标上方；新模板使用轮廓相关匹配，已采王城帧均落到对应图标中心，公会/悬赏/地图帧未达到阈值。道具店实拍是 Bonus 橙色，轮廓算法不固定 RGB；常态白色仍缺真实截图，不能宣称实机已验证。新荒屋模板已接入原生跳轮入口，实机点击尚待复核。图标只能定位城市动作和任意城市主界面，具体王城身份仍只由塔楼背景确认。旧/疑似未引用素材索引见 [deprecated-assets.md](../next/docs/deprecated-assets.md)。
- 本轮未继续执行公会领取及后续任务。静态盘点确认 `guildRequest / Bounties / CompletionReported / GotoDung / beginningAbyss / B2FTemple` 等仍是英文文字模板，完整繁中蝎女链仍未交付；后续必须按同一语义资源契约补齐，不能继续在业务函数中逐节点追加语言特例。
- 后续人工采集了旅店“住宿”及四个房型/返回，共 6 张繁中名称素材；又按郊外主列表从火之魔窟到別離洞窟逐项进入楼层菜单，保存 29 张名称素材。所有郊外选项统一只裁文字、限制 ROI 右缘至 x=795 以内，排除两侧箭头与随机骷髅。图片与语义资源已索引到源码和运行包源目录；原生住宿/郊外任务链没有因素材增加而完成切换，交付目录未重新构建或替换。采集清单和边界见 `next/.local/real-scene-20260923/README.md`；当时未进入副本或购买住宿。
- 圣堂哈肯加护页已实拍：三项随机加护和底部“什麼都不做”。按用户指定只选第一项一次，实证后继是哈肯楼层列表；再点“歸還”到郊外副本列表，点“回到城鎮”才到王城。四张阶段帧与四张稳定模板已保存并索引。新版原生链已增加繁中加护页识别、首项选择及哈肯逐级退场，受影响原生目标编译通过；四帧的阶段识别互不误认。**正式自动任务未重跑、交付目录未替换，不能宣称这条自动链已实机验收。**

- 当前任务配置为 `[悬赏]蝎女`、`法术+地裂`、`ACTIVE_BEAUTIFUL_ORE=true`。运行链把一次受控输入和后续页面观察分成两个节点，任务启动先按冻结配置连接设备；繁中下载按钮与王城塔楼背景均已在实机命中。
- 因果轮实机运行在完成跳跃后曾误报 `SCENE_NOT_FOUND`：子流程只识别英文城市场景。现已把已验证的王城背景接入跳跃后的子流程与蝎女外层终点；这项修正尚未重新运行完整任务。
- 前次真实启动链证据保存在本机 `%LOCALAPPDATA%\WvdNext\runs\16464253-853D-4E92-907F-5165118A326E\1`；本轮没有继续公会领取及后续任务。

## 保留边界

- 本轮不以全任务矩阵作为交付前置；具体游戏任务效果仍由用户从完成的界面实操后按实际问题修复。
- 动作、观察、业务确认已分层；后续实机只针对已补齐的具体页面做有界验证，不通过扩大超时、放宽阈值或重复测试掩盖识别缺口。
- `RESOURCE_UNRESOLVED`、少数复杂识别的成本和任意第三方原生阻塞的取消边界仍保留，不据此声称长期挂机稳定性已经证明。
- 新版是独立可操作版本，尚未替换旧版默认生产入口，也没有新增安装器、自动更新或跨平台实现。
- 最终候选位于 `next/dist/wvd-next`，但在真实四组合和安全任务步骤完成前不得标记为可用版。

## 知识入口

- 使用与构建：[next/README.md](../next/README.md)
- 本轮交付：[windows-functional-delivery.md](../next/docs/windows-functional-delivery.md)
- 架构：[architecture.md](../next/docs/architecture.md)
- 可组合流程状态：[composable-workflow-status.md](../next/docs/composable-workflow-status.md)
- 数据权威：[data-authority.md](../next/docs/data-authority.md)
- 执行注意项：[execution-notes.md](execution-notes.md)
