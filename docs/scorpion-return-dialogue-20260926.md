# 蝎女返城随机剧情中断

## 已确认现场

- 2026-09-26 08:31 启动的新循环完成 5 轮；10:05 第 6 轮中断。
- 第 5 轮日志记录 `Route1_Choose -> Route1_Retreated -> Confirm1 -> HarkenCompleted`，证明战后的快捷返哈肯路线已实际执行。
- 第 6 轮最后输入为 `Task_ReturnGuild_HarkenExit_Outskirts`。游戏已离开郊外，进入王城背景的狮樱对话页；没有出现建筑菜单。
- 旧后置条件仅接受城市菜单，20 秒内无法命中，终态为 `Interrupted / NATIVE_INPUT_RESULT_UNCONFIRMED`。循环保存失败截图并退出，没有重启模拟器或重放归还。
- 本机结果：`%LOCALAPPDATA%/WvdNext/runs/3B232D44-EADD-4893-8E02-C1ACCAB39B29/6/result.json`。
- 现场截图：`%LOCALAPPDATA%/WvdNext/overnight/failure-20260926-083132.png`。

## 修复

- `navigation/harken_exit.cpp` 复用 `ordinary_story_page()` 与 `story_advance_arrow()`，不按人物名称或对话内容匹配。
- 归还后接受城市菜单或普通剧情；剧情是转场发生的证据，不是返城业务终点。
- 剧情分支逐帧识别并点击箭头，每次间隔 700ms，最多 50 次，仍受原 180 秒子图总预算限制。
- 只在建筑菜单出现且普通剧情消失时返回父链。带选项或未知页面不在此自动代选，仍保留失败退出。

## 验证边界

- Release 编译及修改图的定向检查通过。
- 真实失败截图经正式 `WvdVision`/OpenCV 识别：普通剧情 Hit、城市就绪 NoHit、修正后归还后置条件 Hit。
- 没有重新刷整轮；没有将静态识别记作自动点击或剧情续段实机成功。原第 6 轮仍保留 Interrupted。

## 现场续跑

- 12:37 前手动点击当前狮樱普通剧情的继续箭头，确认建筑菜单恢复；没有重启模拟器或调整 VPN。
- 修正版候选已启动新循环，实例 `67CFD846-A85A-44BB-A656-A03DA7D336D5` 的第 1 轮实际进入公会悬赏页并执行 `Task_OldBountyReport_Report`，接续上轮待提交报告。
- 新守护日志为 `%LOCALAPPDATA%/WvdNext/overnight/scorpion-20260926-123706.log`。手动结束当前剧情不等于自动返城剧情分支已经实机通过；该分支等待自然触发。

## 网络覆盖层续修

- 新循环第 1 轮在 `Task_TimeLeap_RuinsEn@await` 中断，失败帧为 `failure-20260926-123706.png`：繁中正文“發生錯誤，請確認網路環境後再試一次。”及“重試”按钮。它不是游戏卡死，也不是住宿或达成报告失败。
- 原启动流程只有英文重试模板；业务等待没有默认网络事件。新素材分别裁切完整网络正文和按钮文字，固定 ROI，不能只识别通用“错误”或确认按钮。
- `with_boot_recovery()` 为正式内置任务及作者流程封装同一网络事件，输入的 `@await` 继承事件。沿用 `FlowExecutor` 的覆盖层挂起机制：父动作及其回执保持原位，网络处理时间暂停父等待预算，恢复后必须看到原动作的新帧后置条件。
- 处理器仅点击已确认的重试按钮。按钮消失后等待 3 秒再观察，重新弹出时继续处理；单次等待最多 120 秒、处理器总预算 180 秒、最多 20 次重试。超限仍保存诊断并停止，不无限重试、不因网络问题关闭模拟器。
- 当前现场弹窗已经消失，手动从荒屋“離開”返回王城，准备使用修正版重新循环。静态失败帧与正式图接线只作定向检查，不冒充随机网络故障已经实机恢复。
- 修正版已完成 Release 编译、失败帧正式识别和父输入等待器事件接线检查；候选服务已更新，13:23 启动新的正式循环，日志为 `%LOCALAPPDATA%/WvdNext/overnight/scorpion-20260926-132315.log`。已实际完成悬赏列表检查和跳轮，到达 `Task_TimeLeap_Done -> Task_Leaped`，越过上轮荒屋入口的中断位置；没有重启模拟器或修改 VPN。该轮尚未自然复现网络弹窗。
